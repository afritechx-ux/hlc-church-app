export default function PublicTestPage() {
    return (
        <div style={{ padding: '50px', textAlign: 'center' }}>
            <h1>Public Test Page</h1>
            <p>If you can see this, public routes work!</p>
            <p>No JavaScript, no auth, no dependencies.</p>
        </div>
    );
}
